# Review package: a585c45..HEAD

## Commits
56b4687 docs: 更新编辑能力相关文案

## Files changed
 README.md                           | 30 +++++++++++++++++++----
 STORE.md                            | 48 +++++++++++++++++++++++++++++--------
 package.json                        |  2 +-
 public/_locales/zh_CN/messages.json |  2 +-
 4 files changed, 66 insertions(+), 16 deletions(-)

## Diff
diff --git a/README.md b/README.md
index 866d81b..e215207 100644
--- a/README.md
+++ b/README.md
@@ -1,45 +1,48 @@
 <div align="center">
 
 <img src="docs/screenshots/icon.png" width="72" height="72" alt="Markdown Reader" />
 
 # Markdown Reader
 
-**一款只做一件事的浏览器扩展：把 Markdown 文件变得好看、好读。**
+**一款浏览器扩展：把 Markdown 文件变得好看、好读，需要改的时候也能直接编辑、原地保存。**
 
 不联网、不上传、不注册——文件全程留在你自己的电脑上。
 
 </div>
 
 <br />
 
 <img src="docs/screenshots/01-main-light.png" alt="Markdown Reader 主界面：左侧目录导航，右侧渲染正文" width="100%" />
 
 <br />
 
 ## 这是做什么的
 
 平时打开一份 `.md` 文件（比如项目里的 `README.md`、笔记软件导出的文档），浏览器要么直接显示成一堆夹杂 `#` 和 `**` 符号的纯文本，要么得专门开个编辑器才能看。
 
 装上 Markdown Reader 之后，浏览器打开的每一份 `.md` 文件都会自动变成一篇排版整齐的文章：标题分级清楚、代码带语法高亮、表格对齐、图表能画出来——而且**全程在本地完成，不产生任何网络请求**，你的文件内容不会离开这台电脑。
 
+需要改内容的时候，按 `⌘E`（Windows / Linux 是 `Ctrl+E`）就能就地编辑，写完停笔自动存回原文件——保存走的是浏览器本地的文件系统接口，同样不涉及任何网络请求。
+
 ## 主要功能
 
 | | |
 | --- | --- |
 | 📖 **完整渲染** | 表格、任务清单 `- [x]`、脚注、上下标、高亮 `==文字==` 等 GitHub 风格 Markdown 语法全支持 |
+| ✏️ **就地编辑** | 按 `⌘E` 进入编辑，正文保持渲染态，只有正在改的那一段显示 Markdown 源码；改完自动写回原文件，**除改动处外全文逐字节不变** |
 | 🎨 **代码高亮** | 200 多种编程语言，颜色自动跟随浅色 / 深色模式切换 |
 | 📊 **图表与公式** | 写 ` ```mermaid ` 代码块自动画成流程图，`$E=mc^2$` 自动渲成数学公式 |
 | 📑 **自动目录** | 侧栏按标题自动生成目录树，滚动阅读时同步高亮当前章节 |
 | 📂 **同目录跳转** | 打开一份文档，侧栏自动列出它所在文件夹里的其他 `.md`，点一下就切换 |
 | 🔍 **全文查找** | `Ctrl/Cmd + F` 呼出查找框，支持上一条 / 下一条跳转与命中计数 |
-| 🔄 **自动刷新** | 用别的编辑器改完文件保存，页面自动重新渲染，阅读位置不会跳动 |
+| 🔄 **自动刷新** | 文件在别处被改动（脚本生成、`git checkout`、命令行编辑等），页面自动重新渲染，阅读位置不会跳动；编辑器自己写回的改动不会触发这个提示 |
 | 📤 **随时导出** | 一键导出成独立 HTML 网页、整理过的 Markdown 文件，或打印成 PDF |
 | 🖌️ **四套主题** | 内置四种阅读配色，正文宽度、字号、行距均可调，也支持写自定义 CSS |
 | 🔒 **零数据收集** | 不发起任何网络请求，所有设置只存在你自己的浏览器里 |
 
 ## 安装
 
 商店上架前，需要从源码构建后手动加载，全程不到两分钟（需要先装好 [Node.js](https://nodejs.org/)）。
 
 1. 下载本仓库源码，在项目目录下执行：
    ```bash
@@ -62,45 +65,64 @@
 ### 方式二：直接拖拽
 
 把 `.md` 文件从文件夹里拖进阅读器窗口，松手就渲染。
 
 ### 方式三：在浏览器里直接打开 `.md`（推荐）
 
 装好扩展后，双击本地 `.md` 文件，或者在地址栏访问一个 `.md` 地址，页面会**自动被阅读器接管**，地址栏保持不变——带着 `#标题` 打开还会自动跳到对应章节。
 
 本地文件需要额外授权一次：`chrome://extensions` → 找到本扩展 → 打开「允许访问文件网址」。不想要这个行为，可以在「设置 → 高级」里关掉。
 
+### 编辑与保存
+
+按 `⌘E`（Windows / Linux 是 `Ctrl+E`）进入编辑态：正文仍然是渲染后的样子，只有光标所在的那一段会显示成 Markdown 源码，改完自动挪回渲染态。停笔约 0.8 秒后自动写回原文件，也可以随时按 `⌘S` 立刻保存。
+
+第一次点「编辑」（或第一次保存）时，浏览器会弹出一次写入权限确认。同意后之后的保存都是静默写回；拒绝也不影响继续编辑，只是保存会变成「另存为」一份新文件，原文件不会被碰。
+
+**通过「方式三」（浏览器直接打开）进入的页面无法写回原文件**：那里的阅读器跑在一个跨源 iframe 里，浏览器不允许它拿到文件句柄，也弹不出文件选择器，`⌘S` 只能触发下载一份改过的文件，不会覆盖你双击打开的那份原文件。想要能直接写回，请用「方式一」或「方式二」打开文件。
+
+**自动保存写的是磁盘上的真实文件，没有回收站。** 编辑器会在内存里保留最近几个「保存前」的版本供你退回（保留个数可在设置里调整），但这份缓冲只存在于当前标签页的内存里——刷新或关掉页面就会丢失，不会被写进任何本地存储。想要长期的修改历史，请配合 Git 或其他版本管理工具。
+
 <br />
 
 <img src="docs/screenshots/04-mermaid-math.png" alt="Mermaid 流程图与数学公式渲染效果" width="100%" />
 
 <p align="center"><sub>写在 Markdown 里的代码块、公式和 Mermaid 图表，打开即所见即所得</sub></p>
 
 <br />
 
 ## 个性化设置
 
-点右上角齿轮图标打开设置面板：明暗模式、阅读主题、正文宽度、字号行距字间距，改动都是实时生效，不用刷新页面。
+点右上角齿轮图标打开设置面板：明暗模式、阅读主题、正文宽度、字号行距字间距，改动都是实时生效，不用刷新页面。「编辑」分组里可以关闭自动保存、调整自动保存的停笔延迟、设定打开文档时默认进入编辑还是阅读态、编辑态是否显示行号与缩进方式，以及内存版本缓冲保留几份。
 
 <img src="docs/screenshots/03-settings.png" alt="设置面板：外观与 Markdown 渲染选项" width="100%" />
 
 深色模式下代码高亮、图表配色都会跟着自动换一套配色，不会出现刺眼的白底代码块：
 
 <img src="docs/screenshots/02-main-dark.png" alt="深色模式下的阅读界面" width="100%" />
 
 ## 常见问题
 
 **为什么双击 `.md` 文件没有变成阅读器界面？**
 检查 `chrome://extensions` 里是否开启了「允许访问文件网址」；这是浏览器本身对本地文件访问的限制，扩展没有这个权限就读不到文件内容。
 
 **打开的是下载对话框，不是阅读器？**
 个别网站给 `.md` 文件加了强制下载的响应头，浏览器会直接下载而不打开页面。这种情况用工具栏的「打开文件」手动选择下载好的文件即可正常阅读。
 
 **我的文件内容会被上传吗？**
-不会。这个扩展不包含任何网络请求代码，也不会向任何服务器发送数据。所有设置和阅读记录只保存在你自己的浏览器本地存储里。
+不会。这个扩展不包含任何网络请求代码，也不会向任何服务器发送数据。所有设置和阅读记录只保存在你自己的浏览器本地存储里；编辑与保存也是纯本地操作，走的是浏览器提供的文件系统接口，不涉及网络。
+
+**编辑后点了「拒绝」写入权限，改的内容还在吗？**
+在。拒绝只影响保存的落点——保存会弹出「另存为」让你存成一份新文件，正在编辑的内容不会丢，原文件也不会被动。
+
+**双击打开的 `.md` 编辑后为什么按 `⌘S` 变成了下载？**
+双击 / 地址栏直接打开的页面运行在跨源 iframe 里，浏览器不允许它获取文件句柄或弹出选择器，所以这条路径下没有「写回原文件」这个选项，`⌘S` 只能下载一份改过的文件。想要能直接写回，请改用工具栏「打开文件」或拖拽打开。
+
+**自动保存会不会把我改错的内容存丢，能撤销吗？**
+编辑器本身的撤销（`⌘Z`）随时可用；此外每次保存前都会在内存里留一份「保存前」的版本（可在设置里调整保留几份），可以用来找回上一次保存前的内容。但这份缓冲只在当前标签页的内存里，刷新页面就会清空，也不会写入本地存储——它不是持久的历史记录，只是当次编辑会话内的后悔药。
 
 **支持 Windows / Mac 吗？**
 支持所有安装了 Chrome 或其他 Chromium 内核浏览器（Edge、Brave 等）的系统。
 
 ## 反馈
 
 用着有问题，或者想要什么功能，欢迎提 [Issue](https://github.com/suyue1000/md-reader/issues)。
diff --git a/STORE.md b/STORE.md
index d8bbfa2..9cedfd2 100644
--- a/STORE.md
+++ b/STORE.md
@@ -5,67 +5,83 @@
 上传入口：<https://chrome.google.com/webstore/devconsole>（需 Google 账号 + 一次性 $5 注册费）
 
 ---
 
 ## 一、商店信息
 
 **名称**：Markdown Reader
 
 **简介**（132 字符以内）
 
-> 轻量、快速的本地 Markdown 阅读器：GFM 渲染、目录导航、自动刷新、完全离线。
+> 轻量、快速的本地 Markdown 阅读器与编辑器：GFM 渲染、就地编辑并写回原文件、目录导航、完全离线。
 
 **详细描述**
 
 ```
-一个专注阅读的本地 Markdown 阅读器。打开文件就能看，不上传、不联网、不注册。
+一个专注本地文件的 Markdown 阅读器与编辑器。打开文件就能看，需要改的时候直接编辑、原地保存；不上传、不联网、不注册。
 
 主要功能
 · 完整 GFM 渲染：表格、任务列表、脚注、定义列表、上下标、高亮
+· 就地编辑：正文保持渲染态，只有正在改的那一段显示 Markdown 源码，改完自动写回原文件，除改动处外全文逐字节不变
 · 语法高亮：200+ 语言，浅色/深色配色自动跟随主题
 · Mermaid 图表与 KaTeX 数学公式
 · 目录导航：自动抽取标题树，滚动时高亮当前章节
-· 在浏览器里直接打开 .md 文件即自动渲染，地址栏保持不变
+· 在浏览器里直接打开 .md 文件即自动渲染，地址栏保持不变（此入口暂不支持写回原文件，编辑后保存会改为下载）
 · 侧栏自动列出文档所在目录，可在同一文件夹的文档间跳转
 · 全文查找：Ctrl/Cmd+F，支持上下跳转与计数
-· 自动刷新：外部编辑器改动文件后自动重新渲染，并保持阅读位置
+· 自动刷新：文件在别处被改动后自动重新渲染并保持阅读位置；编辑中与外部改动冲突时会提示选择，不会静默覆盖
 · 导出：独立 HTML（样式内嵌）、规整后的 Markdown、PDF（经浏览器打印）
 · 四套阅读主题、十档正文宽度、字号行距字间距可调、支持自定义 CSS
 · 大文档友好：分块渐进渲染，10MB 文档首屏约 0.24 秒且全程可滚动
 
 隐私
-本扩展不收集任何数据，不发起任何网络请求。所有设置只保存在你自己的浏览器里。
+本扩展不收集任何数据，不发起任何网络请求。编辑与保存同样是纯本地操作，走浏览器提供的文件系统接口读写你自己的文件，不经过网络、不上传到任何服务器。所有设置只保存在你自己的浏览器里。
 
 界面语言：简体中文
 ```
 
 **分类**：工具（Tools）／生产力（Productivity）
 
 **语言**：中文（简体）
 
 ---
 
 ## 二、权限说明（审核必答）
 
 审核会逐条询问权限用途，下面可直接粘贴。
 
 ### `storage`
 
-> 保存用户的阅读偏好（主题、字号、正文宽度、自定义 CSS）、最近打开的文件列表，
-> 以及每篇文档的阅读位置，以便下次打开时回到原处。数据只存于本机浏览器，不上传。
+> 保存用户的阅读与编辑偏好（主题、字号、正文宽度、自定义 CSS；是否自动保存、
+> 自动保存的停笔延迟、打开文档时默认进入编辑还是阅读态、编辑态是否显示行号、
+> 缩进方式、内存版本缓冲保留几份）、最近打开的文件列表，以及每篇文档的阅读
+> 位置，以便下次打开时回到原处。这里存的都是设置数值本身，不包含文档内容：
+> 编辑时的历史版本只保留在内存里，不写入 `storage`，刷新页面即丢失。数据只
+> 存于本机浏览器，不上传。
 
 ### `host_permissions: file:///*`
 
 > 用于读取用户主动打开的本地 Markdown 文件，以及列出该文件所在的目录，
 > 供用户在同一文件夹的文档之间跳转。此权限还需用户在扩展详情页手动开启
 > 「允许访问文件网址」才会生效，扩展不会、也无法读取该目录之外的内容。
 
+### 文件写入（File System Access API，不在 manifest 权限清单里）
+
+> 本扩展现在支持就地编辑：用户按快捷键进入编辑态、修改文字后，扩展会把改
+> 动写回用户自己刚打开的那份文件。这不是一条 manifest 权限，而是浏览器原生
+> 的 File System Access API——写入前浏览器会向用户弹出一次原生的写入权限
+> 确认，用户同意后才会写盘；用户拒绝也不影响继续编辑，只是保存会退化成
+> 「另存为」一份新文件，原文件不会被触碰。写入目标始终是用户主动选中并已
+> 经在读的那一份文件，扩展不会、也无法写入用户没有主动打开过的路径。此外，
+> 通过「浏览器直接打开 .md」这条入口进入的页面运行在跨源 iframe 中，拿不到
+> 文件句柄也弹不出选择器，因此这条入口下编辑后无法写回原文件，只能下载。
+
 ### 内容脚本 `*://*/*.md` 与 `file:///*.md`
 
 > 仅匹配路径以 .md / .markdown / .mdown / .mkd 结尾的地址。
 > 浏览器默认把这类文件显示为纯文本，脚本将其替换为渲染后的阅读界面。
 > 脚本不读取、不修改任何其他页面。
 
 ### 远程代码
 
 > **不使用**。全部代码随扩展包一同分发，无远程加载、无 eval。
 
@@ -85,20 +101,22 @@
 
 ```
 Markdown Reader 隐私政策
 
 本扩展不收集、不存储、不传输任何用户数据。
 
 · 不发起任何网络请求
 · 所有设置与阅读记录仅保存在用户本机的浏览器存储中
 · 不含分析、埋点或广告代码
 · 用户打开的文件内容只在本地内存中处理，不会离开设备
+· 编辑功能的保存与自动保存同样是纯本地操作，写入的是用户自己选中的那份文件，不经过网络
+· 编辑时的历史版本仅保留在当前标签页的内存中，刷新或关闭页面即丢失，不会被写入任何本地存储
 
 联系方式：<你的邮箱>
 ```
 
 托管方式任选：GitHub 仓库里的一个 Markdown 文件、GitHub Pages、Gist。
 
 ---
 
 ## 四、素材清单
 
@@ -128,23 +146,33 @@ Markdown Reader 隐私政策
 - manifest 引用的文件确实存在于产物中
 - `__MSG_*__` 占位符在 messages.json 里都有定义
 - 产物中不含 sourcemap
 
 需要人工确认的：
 
 - [ ] 版本号已递增（商店不接受重复版本）
 - [ ] 已准备好隐私政策链接
 - [ ] 已准备好截图
 - [ ] 用真实场景走查一遍：打开本地 .md、切主题、查找、导出
+- [ ] 编辑走查：进入编辑态改一处文字确认自动写回原文件、拒绝写入权限后确认改成「另存为」、
+      在「浏览器直接打开 .md」入口下确认编辑后 ⌘S 是下载而不是静默失败
 
 ---
 
 ## 六、已知的审核关注点
 
 **`host_permissions: file:///*` 会触发人工审核。** 这是本扩展最敏感的一项，
-审核周期可能从几小时拉长到数天。用途说明必须写清楚「只读用户主动打开的文件及其所在目录」。
-
-若希望降低审核摩擦，可以考虑把它改为 `optional_host_permissions`，
+审核周期可能从几小时拉长到数天。用途说明必须写清楚这项权限用于读取用户主动
+打开的文件及其所在目录，**不能再写「只读」**——扩展现在支持编辑，会在用户
+明确操作（进入编辑态、保存）下把改动写回用户自己打开的那份文件。审核如果
+追问写入能力，对应的用途说明见上面「文件写入（File System Access API）」
+一节：写入不是这条 manifest 权限本身带来的，而是浏览器原生 API 在用户手势
+下另外弹出的写入授权，但既然它作用在同一份文件上，审核大概率会一并问到，
+提交前建议把这段说明也备好。
+
+若希望降低审核摩擦，可以考虑把 `host_permissions` 改为 `optional_host_permissions`，
 在用户首次需要「列出所在目录」时再申请。代价是多一次授权交互，
 且需要改动 `useLocalDirectory` 的失败分支去触发 `chrome.permissions.request()`。
+这个改动不影响文件写入那部分——写入走的是独立的 File System Access API 授权，
+与 `host_permissions` 无关。
 
 **首次提交通常比后续更新慢。** 预留几天。
diff --git a/package.json b/package.json
index 4cc5c70..ea9d4c3 100644
--- a/package.json
+++ b/package.json
@@ -1,16 +1,16 @@
 {
   "name": "md-reader",
   "displayName": "Markdown Reader",
   "version": "0.1.0",
   "private": true,
-  "description": "轻量、快速的本地 Markdown 阅读器 Chrome 扩展",
+  "description": "轻量、快速的本地 Markdown 阅读器与编辑器 Chrome 扩展",
   "type": "module",
   "engines": {
     "node": ">=20"
   },
   "scripts": {
     "dev": "vite build --watch --mode development",
     "build": "npm run icons && tsc --noEmit && vite build && vite build --config vite.content.config.ts",
     "preview": "vite preview",
     "icons": "node scripts/gen-icons.mjs",
     "typecheck": "tsc --noEmit",
diff --git a/public/_locales/zh_CN/messages.json b/public/_locales/zh_CN/messages.json
index d164f4d..1c605c6 100644
--- a/public/_locales/zh_CN/messages.json
+++ b/public/_locales/zh_CN/messages.json
@@ -1,10 +1,10 @@
 {
   "extName": {
     "message": "Markdown Reader",
     "description": "扩展名称"
   },
   "extDescription": {
-    "message": "轻量、快速的本地 Markdown 阅读器：GFM 渲染、目录导航、自动刷新、完全离线。",
+    "message": "轻量、快速的本地 Markdown 阅读器与编辑器：GFM 渲染、就地编辑并写回原文件、完全离线。",
     "description": "扩展描述"
   }
 }
