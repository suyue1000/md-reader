55:Ruling R1: 不新建 git worktree，在已有的 `feat/editor` 分支上执行 — 仓库里 `node_modules` 与 `dist` 已就绪，worktree 需重装依赖且本次是长链路任务；分支本身已与 `main` 隔离，满足"不在主分支上实现"的底线 — 代价：这段时间内无法在同一仓库并行开发别的功能。
57:Ruling R2 (F2): T5 以"收尾后"的 `ViewPlugin.fromClass` 版本为准；删除模块级 `activeCache` 与 `cacheOf()`，`buildDecorations` 签名改为 `(view, cache)` — 第一版是残稿，模块级单例在同时存在两个编辑器实例时会串味 — 代价：无，第一版本就不可编译。
59:Ruling R3 (F3): 只读切换必须用 `Compartment`，不能用 `EditorView.editable.reconfigure()` — 后者是 `Compartment` 的方法，Facet 上没有，照抄会编译失败 — 代价：不提前拦截的话 T6 会浪费一整轮修复循环。
61:Ruling R4 (F4): T6 的 Files 补上 `src/editor/block-cache.ts` 与 `block-cache.test.ts`（加 `node.dataset.blockKey = key` 及其断言） — 代价：无。
63:Ruling R5 (F1，真雷): T13 的 `edit` 动作与 T17 的 `save` 动作必须带 `allowInInput: true` — 已核实 `useHotkeys.ts:46` 会在事件目标可编辑时跳过绑定，而 `isEditableTarget` 对 `isContentEditable` 返回 true，CodeMirror 的 `.cm-content` 正是 contenteditable；不改的话编辑态下 `⌘S` 与 `⌘E` 完全失效——恰恰是最需要它们的时候 — 代价：不改则核心功能不可用；改了之后这两个键在任何输入框里都会生效，可接受（保存与切模式本就是全局动作）。
65:Ruling R6 (F5): T9 删除 `highlight.ts` 后，"所有命中高亮可见"是硬要求；若 `@codemirror/search` 的扩展在不打开其内置面板时不提供全量命中高亮，实现者需自写 decoration 补上，不得以"CM 自带"为由交付一个没有高亮的查找 — 代价：可能增加 T9 的工作量。
67:Ruling R7 (F6): T16 中 `src/utils/auto-refresh.ts` 及其测试是**删除**，`decideRefresh` 是 `decideRefreshAction` 的超集 — 代价：无。
69:Ruling R8 (F7): 不新建 `markdownRenderer` 单例，改为把渲染器**作为参数注入**：
81:Ruling R9 (Task 1 Important，plan-mandated): 该注释文本是计划逐字指定的，但断言与当下事实不符。不删句、不回退阈值（阈值提前调高是对的，Task 6 就要用），改为前瞻式表述「编辑器视图接入后，入口会涨到 350KB 量级」 — 理由：阈值与说明是两件事，说明必须在任何一个提交上都成立 — 代价：若最终体积远超 350KB，这句仍需在阶段 A 验收时复核。
90:Ruling R10 (Task 2 Important，plan-mandated): 结构性修复不放在 T2。T2 只补注释写明「trailing 块不可用行区间标识」这一假设；真正的修复放到 T3——trailing 块的缓存键改用**数组下标**而非 startLine，天然唯一 — 理由：行区间对 trailing 块本就无意义，在产出端硬造一个假位置不如在消费端用真正唯一的东西；且 T3 才是形成缓存键的地方 — 代价：若 T3 实现者忽略这条，缺陷会潜伏到出现第二个 trailing 块的那天（当前无插件会产出）。已列入 T3 dispatch 必带项。
91:Ruling R11 (Task 2 两条 Minor): 破例并入本轮修复而非按惯例推迟 — 两条都只动注释与新增一个用例，零风险，且 block-slice 是整个功能的地基，嵌套容器用例的价值高于流程洁癖 — 代价：本轮修复范围略大于严格意义上的 Important-only。
104:Ruling R12 (与 R11 相反的取舍): Task 3 的三条 Minor 全部推迟，不并入修复循环 — 与 T2 不同，这三条都是纯文档/覆盖增强，且无功能风险；T2 那次破例是因为 block-slice 是地基且改动零风险，若每次都破例就等于取消了"Minor 不进循环"这条规则 — 代价：这三条要等到最终审查的修复波才处理，期间代码里缺两句有价值的注释。已列入最终审查的分诊清单。
105:Ruling R13: 把 `node.dataset.blockKey = key` 及其断言从 Task 6 移到 Task 4（block-cache.ts 的创建者）执行 — 理由：字段属于该文件本身，在创建它的任务里一次写全，好过后面再回访一次；且 Task 6 已是阶段 A 最大的任务，能减负则减 — 代价：Task 4 的 diff 会略超出其简报原文，需在派发审查时说明这是控制方裁决而非范围蔓延。
127:Ruling R14 (Task 6 派发前发现，计划未考虑): 滚动归属权 —— 让 **<main> 继续做滚动容器**，编辑器随内容自然增高，而不是照简报让 CodeMirror 内部滚动。
132:Ruling R15 (Task 6 派发前发现): 简报要求删掉 contentKey / enhancedToken 等分块渲染脚手架，但 useScrollSpy / useReadingPosition / usePendingAnchor 三个 hook 仍在消费它们，直接删会让 typecheck 挂。Task 6 改为搭一座**临时桥**：ReaderPage 维护一个 renderToken（每轮块渲染完成后自增），以它派生 contentKey 并喂给这三个 hook，且必须注释标明是临时接线、由 Task 7/8 替换 — 理由：阶段 A 的每个任务都要保持 verify 通过，不能留一个编译不过的中间态 — 代价：Task 7/8 必须真的把这座桥拆掉，否则会留下一个语义含糊的 token；已列入 T7/T8 的派发必带项。
138:Ruling R16 (推翻 R2): 装饰产出方式改为**状态派生** `EditorView.decorations.compute([blocksField,'doc','selection',EditorView.editable], ...)`，
155:Ruling R17: 上述七项不在 Task 6 修复，各自留给已排期的任务 — 理由：把它们塞进 Task 6 等于把 T7-T10 合并进来，正是分阶段要避免的事；且计划本就规定阶段 A 结束（Task 11）才做整体验收 — 代价：Task 6 到 Task 10 之间，目录跳转/搜索/导出/打印四项功能实际处于损坏状态，分支中途不可用。这个代价必须让用户知道。已列入 Task 11 阶段 A 验收的逐条复核清单。
159:Ruling R18: `usePendingAnchor` 在整份计划里**没有任何任务负责迁移**——这是计划缺口，不是实现缺陷。并入 Task 8 范围（它已在改目录跳转，同属「按行号定位」这件事）。README 明确宣传了 `xxx.md#锚点` 这个用法，不能留着坏掉 — 代价：Task 8 范围变大。
165:Ruling R19: 只有这三条进本轮修复循环 — 前两类都不是本任务能或该解决的 — 代价：无。
188:Ruling R20 (我自己的指令有份责任): 我在 Task 7 的派发里写了「行号定位不依赖高度，等待不再需要」——这句话是错的，它混淆了**记录**与**恢复**：只有记录不依赖高度，恢复要把行号换算回像素，必须等布局稳定。
198:Ruling R21: 三条全部进第 2 轮修复，不推迟 — (7) 是本轮修复自己引入的架构性依赖，按规则必须并入未决清单；(4) 复审给了具体且不引入竞态的做法，属于「有更好解法且成本低」，不采纳说不过去；(2) 只需如实标注为已知降级，成本近乎为零 — 代价：Task 7 多一轮循环。
250:Ruling R22: 正文 `#锚点` 链接损坏（T8 新发现的计划缺口）不并入 T8 修复轮，单开一项处理 —— 理由：修复轮只该处理 findings，扩大范围会让复审失去焦点；且该缺口与 usePendingAnchor 同源，T8 完结后由同一实现者接手时上下文仍新鲜 — 代价：阶段 A 多一个计划外任务（约 15 分钟）。编号 Task 8b，排在 T8 完结之后、T9 之前。
254:Ruling R23 (归并): 「带锚点打开时落点比目录点击少滚约 8px，导致侧栏高亮落在上一节」并入 **Task 8b**，不并入本修复轮。
260:Ruling R24 (保留): onEnhanced 里那道「看似冗余」的守卫予以保留 —— 实现者指出单删它不会让任何测试失败（单删恢复那道守卫会失败 4 例）。保留的理由是让「锚点优先」在两个出口各自独立成立，而不是靠另一个 ref 的副作用间接保证；注释已写明其冗余是设计使然 — 代价：存在一行没有被独立测试扎住的代码；接受，因为它是显式标注的纵深防御，不是被遗漏的分支。
262:Ruling R25 (待复审确认): isScrolledToBottom 的判据被改动（scrollTop > 0 → 可滚余量是否超过容差），属超出「抽函数」要求的行为改变。已在定向复审中要求独立判断其在正常文档与退化输入下的表现 — 若复审判为应回退，则进第 2 轮 — 代价：暂缓结论。
268:Ruling R25 结清: isScrolledToBottom 的判据改变判为**中性可接受，不回退** —— 对正常可滚文档与旧判据完全等价（remainder<=tolerance 成立时 scrollTop>0 自动满足）；差异仅在「内容仅比视口高 1-2px」的退化输入，而该场景下整篇几乎全可见、顶边探测本就能正确定位。已充分披露 + 有专项测试 + 风险可忽略。
270:Ruling R24 补充（复审给出更锐利的定性）: onEnhanced 里那道锚点守卫**实际是死代码**，不只是「未被独立测试」—— 只要闩锁生效，restoredDocRef 永不置为该文档 id，onEnhanced 会先被 restoredDocRef 挡住，锚点守卫从不成为决定性条件。仍维持保留裁决（显式标注的纵深防御），但归类从「缺测试」修正为「不可达分支」，已列入最终审查分诊清单。
297:Ruling R26 (hash 同步的定性，以审查为准): 审查给出比实现者更精确的判断 —— 宿主侧一直只用 replaceState（content/index.ts:218），从未支持前进/后退，故**无增量历史损失**；真实损失是「复制地址栏分享/收藏会丢失新落点」，这与 content/index.ts 里明确宣传的「地址栏保持 file:// 且可分享」有直接冲突。且该缺陷**Task 8 就已引入**（侧栏目录点击），8b 只是扩展到正文锚点，不是全新问题 — 处置：单开任务修，不阻塞 8b 验收。
301:Ruling R27: 新建 **Task 8c —— 定位机制修复**，合并三个已确认缺陷，优先级高于原计划的 Task 9。
322:Ruling R28 (我的疏漏，记录在案): 执行前冲突扫描里我记录了 ReadingPosition 的类型改动，但**只当成类型问题，没问「已存在于用户磁盘上的旧数据怎么办」**。单元测试永远抓不到这类 bug，因为测试环境不存在上一版本的遗留数据。
340:Ruling R29: 把 8c 的三条 Important 与上述两个既有缺陷合并成一次派发（Task 8d），一轮实现一轮审查 —— 理由：8c 原实现者已被中止无法 resume，本就要新起代理；五项都在同一子系统（定位/块缓存），分两轮会多付一整轮审查成本；且缺陷(甲)会让 T11 的「阶段 A 功能等价」验收直接不通过，必须在 T11 之前修 — 代价：单次 diff 变大，审查焦点需明确划定。
360:Ruling R30: 上述 Minor 破例并入修复轮（连同 strayLineBoxes 重复计数、两处被自己发现推翻的旧注释）—— 理由与 R11 同：它保护的是本次交付的核心修复之一，不是锦上添花；而 R12 推迟的那些是纯文档增强 — 代价：本轮范围略大。
376:Ruling R31 (<br> 分歧裁定): **实现者对，审查意见错**。复审用真实 dist CSS 独立搭探针页实测：软换行段落 94.3px、</strong>+软换行 94.3px（确属重复计数，已正确排除）、<br>+软换行 121.5px——相差恰好 27.2px = 一个行盒。<br> 自己断一行、其后的换行在 break-spaces 下再断一次，两者不重复，保留在白名单内正确。
396:Ruling R32 (推翻实现者的论证): 实现者「实测立刻点目录没问题」的理由**无效** —— 「立刻」= 延迟≈0，而其自己的扫描表显示 0/200ms 在对照构建上也通过，失败只在 700–1500ms。实测证据落在已知窗口之外，等于没测。
408:Ruling R33 (裁决实现者主动提出的一处预防性改动): BackToTop 的接线**予以保留** —— 实现者如实报告它在本机没有实测到实际损坏（平滑滚动派发的真实 scroll 事件恰好抢在校正前面），依据只是判定标准第 3 条 + 单元用例。
418:Ruling R33 经复审独立确认：BackToTop 预防性接线保留正确，无副作用（markNavigation 只在真滚动时调用，各 hook 独立维护 baseline，不会连锁撤掉别处判断；用户点「回顶部」后不再被拉回历史位置正是期望行为）
457:Ruling R34: 「要不要把 SVG 纳入高亮范围」进**最终审查分诊** —— 它现在是一处可讨论的取舍而非铁律，且实现者在修复轮里不擅自改行为是对的 — 代价：该取舍悬置到最终审查。
489:Ruling R35 (跨任务影响 · 需最终审查分诊): harness 假阳性诊断**已被审查者核到源码确认**（@codemirror/view/dist/index.js:8327 的视口测量排在 rAF 上，窗口遮挡停 rAF 即不重算视口）。
510:Ruling R36 (体积风险未兑现，需告知用户): viewer 入口实测 **626891 字节**（基线 84030）。spec 风险 1 与 vite.config.ts 注释预算的是「350KB 量级」——**没兑现，实际接近两倍**，是原基线的 7.5 倍。已按实测订正注释（不留没验过的数字在代码里）。
512:Ruling R37 (**纠正我传下去的一条错误指导**): task-10 给出的快捷自检「滚动后第一个 .cm-md-block 还是文档开头 = 踩到 harness 陷阱」在本项目里**恒成立**，是个必然误报——光标默认停在位置 0，CodeMirror 总把选区所在行包进视口范围。
525:Ruling R37 经审查独立确认: 各 scrollTop 下第一个块恒为「# 代码文档」，top 分别 86/−3914/−7914/−11914/−15914（可视区 46~789），与实现者数字分毫不差。**后续派发指令必须改用「最后一个挂载的块」或「第一个矩形落在视口内的块」**。
531:Ruling R38 (阶段 A 收官措辞): 审查判定**不能用「功能与改造前等价」，可按「无已知功能回归」收官** —— 差三件：一次真基线 A/B、跨源 downloadBlob 分支验证、trap 归因查证或降级。
544:Ruling R37 归因已从推断升级为查证: @codemirror/view:6256-6266 的 updateForViewport() 在主选区不在滚动视口内时**另加一个 Viewport** 盖住它那行；@codemirror/state:2762 的默认选区是位置 0 的光标（MarkdownEditor.tsx grep selection 零命中）。DOM 侧指纹：文档第一块之后紧跟一个高 3356px 的 div.cm-gap。起作用的是 EditorState 的选区而非 DOM 选区（实测 getSelection().anchorNode 为 null）
546:Ruling R39 (阶段 A 收官措辞，最终): **「无已知功能回归」**，不用「功能与改造前等价」。依据：对照只覆盖渲染产物一个维度（交互类判据旧版无同样对照数据）、已知差异不为零、实测在替身环境里做、抽样不是全集
556:Ruling R39 经复审确认: 收官措辞「无已知功能回归」**恰如其分**，不算保守也不算过强 —— 精确对应实测覆盖范围，同时诚实列出未覆盖维度
578:Ruling R40 (迁移策略的边界，后续每次改设置结构都适用): deep-merge 兜得住两类 ——「整组新增」（本次）与「组内单字段缺失」；**顶不住类型收窄或语义变化**（字段改类型 / 改名 / 拆分），那类它只做结构合并、不做值校验，会把不合法的旧值原样透传到运行时。
611:Ruling R41 (defaultMode 的处置): 接上它并接受诚实降级 —— 打开即进编辑态时无手势、申请不到写权限，writable 保持 false、状态提示如实说明，等用户第一次按 ⌘S（真手势）时再申请。若实现者判断走不通，则从面板撤掉该项 —— **死设置比没有这个设置更糟** — 代价：多一条降级路径要维护。
647:Ruling R42: **做**——围栏代码块的落点改到开栅栏的**下一行**（即代码内容首行），其余块类型维持块首不变。
656:Ruling R43 (空代码块分歧，我与实现者): **我对，实现者的「风险等价」不成立**。
848:Ruling R44 (mod+shift+e 维持关闭): 采纳实现者的**不对称论证** —— 修完之后没有任何入口会给出错误结果，⌘⇧E 在编辑态不触发属于「安全的失败」；
860:Ruling R45 (残余定级，复审独立推演后): **移交最终审查分诊，不阻塞本轮**。
