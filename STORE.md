# Chrome 应用商店上架材料

打包：`npm run package` → `release/md-reader-v1.0.0.zip`（约 3.6 MB）

上传入口：<https://chrome.google.com/webstore/devconsole>（需 Google 账号 + 一次性 $5 注册费）

---

## 一、商店信息

**名称**：Markdown Reader

**简介**（132 字符以内）

> 轻量、快速的本地 Markdown 阅读器：GFM 渲染、目录导航、自动刷新、完全离线。

**详细描述**

```
一个专注阅读的本地 Markdown 阅读器。打开文件就能看，不上传、不联网、不注册。

主要功能
· 完整 GFM 渲染：表格、任务列表、脚注、定义列表、上下标、高亮
· 语法高亮：200+ 语言，浅色/深色配色自动跟随主题
· Mermaid 图表与 KaTeX 数学公式
· 目录导航：自动抽取标题树，滚动时高亮当前章节
· 在浏览器里直接打开 .md 文件即自动渲染，地址栏保持不变
· 侧栏自动列出文档所在目录，可在同一文件夹的文档间跳转
· 全文查找：Ctrl/Cmd+F，支持上下跳转与计数
· 自动刷新：外部编辑器改动文件后自动重新渲染，并保持阅读位置
· 导出：独立 HTML（样式内嵌）、规整后的 Markdown、PDF（经浏览器打印）
· 四套阅读主题、十档正文宽度、字号行距字间距可调、支持自定义 CSS
· 大文档友好：分块渐进渲染，10MB 文档首屏约 0.24 秒且全程可滚动

隐私
本扩展不收集任何数据，不发起任何网络请求。所有设置只保存在你自己的浏览器里。

界面语言：简体中文
```

**分类**：工具（Tools）／生产力（Productivity）

**语言**：中文（简体）

---

## 二、权限说明（审核必答）

审核会逐条询问权限用途，下面可直接粘贴。

### `storage`

> 保存用户的阅读偏好（主题、字号、正文宽度、自定义 CSS）、最近打开的文件列表，
> 以及每篇文档的阅读位置，以便下次打开时回到原处。数据只存于本机浏览器，不上传。

### `host_permissions: file:///*`

> 用于读取用户主动打开的本地 Markdown 文件，以及列出该文件所在的目录，
> 供用户在同一文件夹的文档之间跳转。此权限还需用户在扩展详情页手动开启
> 「允许访问文件网址」才会生效，扩展不会、也无法读取该目录之外的内容。

### 内容脚本 `*://*/*.md` 与 `file:///*.md`

> 仅匹配路径以 .md / .markdown / .mdown / .mkd 结尾的地址。
> 浏览器默认把这类文件显示为纯文本，脚本将其替换为渲染后的阅读界面。
> 脚本不读取、不修改任何其他页面。

### 远程代码

> **不使用**。全部代码随扩展包一同分发，无远程加载、无 eval。

### 数据用途申报

在「隐私权规范」页勾选：**不收集任何用户数据**，并逐项确认：

- 不出售或转让数据给第三方
- 不将数据用于与核心功能无关的用途
- 不将数据用于判定信用状况或放贷

---

## 三、隐私政策

商店要求提供一个公开可访问的隐私政策链接。本扩展不收集数据，内容可以很短：

```
Markdown Reader 隐私政策

本扩展不收集、不存储、不传输任何用户数据。

· 不发起任何网络请求
· 所有设置与阅读记录仅保存在用户本机的浏览器存储中
· 不含分析、埋点或广告代码
· 用户打开的文件内容只在本地内存中处理，不会离开设备

联系方式：<你的邮箱>
```

托管方式任选：GitHub 仓库里的一个 Markdown 文件、GitHub Pages、Gist。

---

## 四、素材清单

| 素材 | 规格 | 数量 |
| --- | --- | --- |
| 商店图标 | 128×128 PNG | 1（`public/icons/icon-128.png` 已有） |
| 屏幕截图 | 1280×800 或 640×400 PNG | 至少 1，建议 4~5 |
| 小型宣传图 | 440×280 PNG | 可选，影响推荐位曝光 |

建议截图内容：

1. 阅读界面全貌（浅色，带目录侧栏）
2. 深色主题 + 代码高亮
3. Mermaid 图表与数学公式
4. 全文查找（右上角查找条 + 正文高亮）
5. 设置面板

截取方式：`npm run build` 后加载 `dist/`，打开 `fixtures/sample.md`，
把窗口调到 1280×800 截图。

---

## 五、上传前自查

`npm run package` 会自动检查下面几项并在不通过时中断：

- manifest 引用的文件确实存在于产物中
- `__MSG_*__` 占位符在 messages.json 里都有定义
- 产物中不含 sourcemap

需要人工确认的：

- [ ] 版本号已递增（商店不接受重复版本）
- [ ] 已准备好隐私政策链接
- [ ] 已准备好截图
- [ ] 用真实场景走查一遍：打开本地 .md、切主题、查找、导出

---

## 六、已知的审核关注点

**`host_permissions: file:///*` 会触发人工审核。** 这是本扩展最敏感的一项，
审核周期可能从几小时拉长到数天。用途说明必须写清楚「只读用户主动打开的文件及其所在目录」。

若希望降低审核摩擦，可以考虑把它改为 `optional_host_permissions`，
在用户首次需要「列出所在目录」时再申请。代价是多一次授权交互，
且需要改动 `useLocalDirectory` 的失败分支去触发 `chrome.permissions.request()`。

**首次提交通常比后续更新慢。** 预留几天。
